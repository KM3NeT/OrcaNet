# These options define the various properties of a model.
#
# They overwrite the default values which are listed in the definition of the Configuration class.
#
[config]
batchsize = 7
train_logger_display=5
train_logger_flush=-1
